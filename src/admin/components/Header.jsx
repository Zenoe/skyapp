const Header = () => {
  return (
    <div className="bg-white shadow-md p-4 fixed w-full z-10 flex items-center justify-between">
      <h1 className="text-xl font-bold">Admin Dashboard</h1>
      {/* Add more header content like user profile, settings, etc. */}
    </div>
  );
};

export default Header;
